{% extends "User/base.html.twig" %}


{% block group %}   {% endblock %}
{% block event %}   {% endblock %}
{% block home %}   {% endblock %}
{% block quiz %}   {% endblock %}
{% block reclamation %}   {% endblock %}

{% block body %}

<!-- BEGIN: Data List -->
                    <div class="intro-y col-span-12 overflow-auto 2xl:overflow-visible">
                        <table class="table table-report -mt-2">
                            <thead>
                                <tr>
                                    
                                    <th class="whitespace-nowrap"> Delivery ID </th>
                                    <th class="whitespace-nowrap">Email</th>
                                    <th class="whitespace-nowrap">  Adresse</th>
                                 
                                    <th class="text-center whitespace-nowrap">ACTIONS</th>
                                </tr>
                            </thead>
                            <tbody>
                                {%for l in Livraisons %}
                                <tr>
                                    <td class="text-center whitespace-nowrap">{{l.id}}</td>
            
                                    <td class="text-center whitespace-nowrap">{{l.email}}</td>
                                    <td class="text-center whitespace-nowrap">{{l.adresse}}</td>
                                    
                                    <td class="text-center whitespace-nowrap"><a href="{{ path('user_certif', { 'id': l.id }) }}" style="color: #fff; background-color: #35dc72; padding: 8px 16px; text-decoration: none; border-radius: 4px; display: inline-block;" > </a></td>
                                </tr>
                                {% endfor %}

                            </tbody>  
                        </table>
                    
                        <!-- END: Data List -->
                        <head>
    <link rel="stylesheet" href="https://unpkg.com/leaflet@1.3.1/dist/leaflet.css"
        integrity="sha512-Rksm5RenBEKSKFjgI3a41vrjkw4EVPlJ3+OiI65vTjIdo9brlAacEuKOiQ5OFh7cOI1bkDwLqdLw3Zg0cRJAAQ=="
        crossorigin="" />
    <style type="text/css">
        #map {
            /* la carte DOIT avoir une hauteur sinon elle n'apparaît pas */
            height: 400px;
        }
    </style>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <title>Carte</title>
</head>

<section class="page-banner overlay pt-170 pb-220 bg_cover" style="background-image: url('{{ asset('user/SRC/logo.png') }}');
;">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="page-banner-content text-center text-white">
                    <h1 class="page-title">Map</h1>
                </div>
            </div>
        </div>
    </div>
</section>

<section >
 <div class="container">
        <div class="row justify-content-center">
            <div class="col-lg-8">
                <!-- Ici s'affichera la carte -->
                <div id="map" style="height: 400px;"></div>
            </div>
        </div>
    </section>
    </div>

    <!-- Inclure les scripts JavaScript -->
    <!-- Inclure Leaflet -->
    <script src="https://unpkg.com/leaflet@1.3.1/dist/leaflet.js"
        integrity="sha512-/Nsx9X4HebavoBvEBuyp3I7od5tA0UzAxs+j83KgC8PU0kgB4XiK4Lfe4y4cgBtaRJQEIFCW+oC506aPT2L1zw=="
        crossorigin=""></script>
    <!-- Votre script JavaScript -->
    <script type="text/javascript">
        // On initialise la latitude et la longitude de Paris (centre de la carte)
        var lat = 36.82371651185815;
        var lon = 10.159066304559651;
        var macarte = null;
        var villes = {
            "Bizerte": { "lat": 37.28637428983345, "lon": 9.823217029164404 },
            "Jendouba": { "lat": 36.507286750522766, "lon": 8.777346337245712 },
            "Gabes": { "lat": 33.8813733708502, "lon": 10.095791527093706 }
        };

        // Fonction d'initialisation de la carte
        function initMap() {
            // Créer l'objet "macarte" et l'insèrer dans l'élément HTML qui a l'ID "map"
            macarte = L.map('map').setView([lat, lon], 6);
            // Leaflet ne récupère pas les cartes (tiles) sur un serveur par défaut. Nous devons lui préciser où nous souhaitons les récupérer. Ici, openstreetmap.fr
            L.tileLayer('https://{s}.tile.openstreetmap.fr/osmfr/{z}/{x}/{y}.png', {
                // Il est toujours bien de laisser le lien vers la source des données
                attribution: 'données © <a href="//osm.org/copyright">OpenStreetMap</a>/ODbL - rendu <a href="//openstreetmap.fr">OSM France</a>',
                minZoom: 1,
                maxZoom: 20
            }).addTo(macarte);
            var offset = 0;
            dest.forEach(function (destination) {
                var marker = L.marker([villes[destination.state].lat + offset, villes[destination.state].lon + offset]).addTo(macarte);
                marker.bindPopup(destination.name);
                offset -= 0.02;
            });
            

// Modifier la taille de la carte
document.getElementById('map').style.height = '600px';
        }

        window.onload = function () {
            // Fonction d'initialisation qui s'exécute lorsque le DOM est chargé
            initMap();
        };
    </script>
{% endblock %}